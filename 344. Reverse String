class Solution {
    public void reverseString(char[] s) {
        int left=0;
        int end=s.length-1;
       while(left<end){
           char temp=s[end];
           s[end]=s[left];
           s[left]=temp;
           left++;
           end--;
       }
    }
}
